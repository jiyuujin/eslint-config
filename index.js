'use strict'

module.exports = {
    rules: {
        indent: ['error', 4],
        quotes: [2, 'single'],
        'linebreak-style': [2, 'unix'],
        semi: [0, 'never'],
        'no-unused-vars': 1
    }
}